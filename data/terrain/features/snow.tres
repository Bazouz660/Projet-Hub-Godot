[gd_resource type="Resource" script_class="Feature" load_steps=8 format=3 uid="uid://dwvxa6i0ewvw2"]

[ext_resource type="Texture2D" uid="uid://74e1d14d4ih8" path="res://asset/texture/foliage/snow_ball.png" id="1_18vdn"]
[ext_resource type="Script" uid="uid://b17kgm5mvh88b" path="res://scene/terrain/feature.gd" id="2_8fowt"]

[sub_resource type="Shader" id="Shader_5juvr"]
code = "shader_type spatial;
render_mode blend_mix, depth_draw_opaque, cull_back, specular_disabled, diffuse_toon;

uniform vec4 albedo : source_color;
uniform sampler2D texture_albedo : source_color, filter_nearest, repeat_enable;
uniform vec4 backlight : source_color;
uniform vec3 uv1_scale;
uniform vec3 uv1_offset;
uniform float wind_strength : hint_range(0.0, 1.0) = 0.1;
uniform float wind_speed : hint_range(0.0, 10.0) = 1.0;
uniform float wind_scale : hint_range(0.1, 10.0) = 1.0;
uniform sampler2D noise_texture : hint_default_white;
uniform float noise_scale : hint_range(0.1, 10.0) = 1.0;
global uniform vec3 player_position;
uniform float player_influence_radius : hint_range(0.0, 10.0) = 2.0;
uniform float player_influence_strength : hint_range(0.0, 1.0) = 0.5;

void vertex() {
    UV = UV * uv1_scale.xy + uv1_offset.xy;
    vec3 world_pos = (MODEL_MATRIX * vec4(VERTEX, 1.0)).xyz;

    // Find the base position (bottom of the grass blade)
    float grass_height = length(VERTEX);
    vec3 base_pos = world_pos - VERTEX;

    // Calculate wind and noise
    float wind_time = TIME * wind_speed;
    vec2 noise_uv = base_pos.xz * noise_scale * 0.01;
    float noise_value = texture(noise_texture, noise_uv + wind_time * 0.1).r * 2.0 - 1.0;

    float position_variation = sin(base_pos.x * 0.1) * cos(base_pos.z * 0.1) * 2.0;
    float wind_horizontal = sin(wind_time + base_pos.x * wind_scale + base_pos.z * wind_scale + position_variation + noise_value) * wind_strength;
    float wind_depth = cos(wind_time * 0.7 + base_pos.x * wind_scale * 1.2 + base_pos.z * wind_scale + position_variation + noise_value) * wind_strength * 0.5;

    // Calculate player influence
    vec3 to_player = player_position - base_pos;
    float distance_to_player = length(to_player);
    float player_influence = 1.0 - smoothstep(0.0, player_influence_radius, distance_to_player);
    vec3 player_deformation = normalize(to_player) * player_influence * player_influence_strength;

    // Calculate the bend influence based on height
    float vertex_height = VERTEX.y;
    float max_height = grass_height;
    float height_fraction = vertex_height / max_height;

    // The deformation increases as we go up the blade
    float bend_influence = height_fraction * height_fraction; // Square it for more natural bending

    // Calculate the total displacement
    vec3 wind_displacement = vec3(wind_horizontal, 0.0, wind_depth);
    vec3 total_displacement = (wind_displacement - player_deformation) * bend_influence;

    // Apply the displacement
    vec3 new_pos = VERTEX;
    new_pos += total_displacement * max_height;

    // Update vertex position
    VERTEX = new_pos;

    // Billboard matrix setup
    MODELVIEW_MATRIX = VIEW_MATRIX * mat4(
        MAIN_CAM_INV_VIEW_MATRIX[0],
        MAIN_CAM_INV_VIEW_MATRIX[1],
        MAIN_CAM_INV_VIEW_MATRIX[2],
        MODEL_MATRIX[3]
    );
    MODELVIEW_NORMAL_MATRIX = mat3(MODELVIEW_MATRIX);
}

vec3 brighten(vec3 color, float factor) {
  return vec3(
    min(color.r + factor, 1.0),
    min(color.g + factor, 1.0),
    min(color.b + factor, 1.0)
  );
}

void fragment() {
    vec2 base_uv = UV;
    vec4 albedo_tex = texture(texture_albedo, base_uv);

    // Make the color lighter as we go up the blade
    float height = 1.0 - UV.y;
    vec3 color = albedo.rgb;
	vec3 color2 = brighten(color, 0.05);
    color = mix(color, color2, height);

    ALBEDO = color * albedo_tex.rgb;
    ALPHA *= albedo.a * albedo_tex.a;
    ALPHA_SCISSOR_THRESHOLD = 0.5;
    BACKLIGHT = backlight.rgb;
}"

[sub_resource type="FastNoiseLite" id="FastNoiseLite_wdbo7"]
noise_type = 4
frequency = 0.0054
fractal_octaves = 3
domain_warp_fractal_octaves = 1

[sub_resource type="NoiseTexture2D" id="NoiseTexture2D_foxm2"]
seamless = true
noise = SubResource("FastNoiseLite_wdbo7")

[sub_resource type="ShaderMaterial" id="ShaderMaterial_sj1ts"]
render_priority = 0
shader = SubResource("Shader_5juvr")
shader_parameter/albedo = Color(1, 1, 1, 1)
shader_parameter/texture_albedo = ExtResource("1_18vdn")
shader_parameter/backlight = Color(1, 1, 1, 1)
shader_parameter/uv1_scale = Vector3(1, 1, 1)
shader_parameter/uv1_offset = Vector3(0, 0, 0)
shader_parameter/wind_strength = 0.0
shader_parameter/wind_speed = 0.0
shader_parameter/wind_scale = 10.0
shader_parameter/noise_texture = SubResource("NoiseTexture2D_foxm2")
shader_parameter/noise_scale = 10.0
shader_parameter/player_influence_radius = 3.0
shader_parameter/player_influence_strength = 1.0

[sub_resource type="QuadMesh" id="QuadMesh_vhdja"]
material = SubResource("ShaderMaterial_sj1ts")
size = Vector2(0.3, 0.2)
center_offset = Vector3(0, 0.05, 0)

[resource]
script = ExtResource("2_8fowt")
label = "Snow"
type = 1
scenes = Array[PackedScene]([])
min_radius = 0.0
mesh = SubResource("QuadMesh_vhdja")
cast_shadow = 0
receive_biome_color = true
shader_parameter_color_name = "albedo"
random_scale = Vector2(0.9, 1.1)
random_rotation = Vector2(0, 360)
random_offset = Vector3(0.5, 0, 0.5)
follow_normals = false
metadata/_custom_type_script = "uid://b17kgm5mvh88b"
